# 00 — Start Here

## Mission

Implement the approved **Notre méthode** experience inside the current SPIMARIMMO codebase with maximum visual fidelity, motion discipline, accessibility and architectural consistency.

The section must explain how SPIMARIMMO delivers value before, during and after an international property exhibition. It is a B2B operational proof experience for Moroccan property developers—not a decorative agency timeline.

## First implementation gate

Start with the complete static parity of:

> `01 — AVANT / Préparer la demande`

However, define the shared component structure and complete three-phase content schema before writing phase-specific JSX. The three approved images are one system, not three independent mockups.

## Repository safety

Before editing:

1. Inspect the current branch, worktree status, remotes and recent history.
2. Locate repository instructions such as `AGENTS.md`, `CLAUDE.md`, project docs and package scripts.
3. Verify the accepted House of Yellow foundation instead of assuming the latest branch is correct.
4. Treat `hoy-clone-baseline-eng-015` at `e048fdde…` as the previously accepted baseline marker and `3675c0206c0f819e9af0760763627934be7de304` as the previously identified reconstruction base. Confirm both against the actual repository before using either.
5. Do not revive the generic `section → heading → paragraph → CTA → card grid` replacement.
6. Do not reset, overwrite or delete unrelated user work.
7. Use a dedicated branch such as `claude/method-section-parity`.
8. Do not push, merge or open a PR unless the user explicitly asks.

If the accepted foundation cannot be found, stop after the audit and report the exact blocker. Do not improvise a new visual foundation.

## Authority split

- House of Yellow controls craft quality, editorial rhythm, motion discipline and attention to detail.
- SPIMARIMMO controls audience, business logic, content, conversion, proof, CMS, CRM and multilingual requirements.
- The current accepted repository controls existing tokens, fonts, components, navigation and technical conventions.
- The three generated method screens control this section's intended visual outcome.

## Required first checkpoint

Before implementation, report:

```text
WORKFLOW_READY=true|false
Repository path:
Current branch:
Current SHA:
Working tree status:
Accepted baseline/tag found:
Existing method-section files:
Existing design tokens/fonts:
Existing motion stack:
Existing test commands:
Proposed owned files:
Phase 01 plan:
Risks/blockers:
```

`WORKFLOW_READY=true` is allowed only after the repository audit, baseline verification and local validation commands succeed or their exact existing failures are documented.

## Required delivery checkpoint

After each gate, report:

- files changed;
- implementation gate completed;
- commands run and results;
- reference and actual screenshots;
- pixel-diff result;
- accessibility result;
- responsive states validated;
- remaining deltas;
- next eligible gate.

