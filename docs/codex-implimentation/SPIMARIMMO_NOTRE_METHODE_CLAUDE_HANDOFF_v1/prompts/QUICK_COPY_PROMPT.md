# Quick Copy Prompt

Use this short prompt only when the complete package is already present in the repository/workspace.

---

Read the entire `SPIMARIMMO_NOTRE_METHODE_CLAUDE_HANDOFF_v1` package and inspect every included PNG reference.

Implement the `Notre méthode` section as one locked three-state system. Begin with Phase 01 static parity at `1536 × 1024`, but create the complete Phase 01/02/03 data model and shared architecture first so later phases cannot drift.

Preserve the accepted House of Yellow-derived SPIMAR foundation, existing tokens, fonts, motion system and unrelated user work. Do not produce a generic timeline/card grid, another phone mockup, fake metrics or invented content.

Before editing, return the `WORKFLOW_READY` checkpoint specified in `00_START_HERE.md`. After implementation, stop at the Phase 01 parity gate and return the evidence report required by `prompts/CLAUDE_PHASE_01_KICKOFF_PROMPT.md`.

Do not push or merge without explicit authorization.

---

